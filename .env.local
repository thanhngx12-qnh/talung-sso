# .env.local
NODE_ENV=development

# Database
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=password
POSTGRES_PORT=5433  # Thêm dòng này

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_URL=http://localhost:8081  # Đổi từ 8080 sang 8081
KEYCLOAK_REALM=talung

# Admin API
API_PORT=3001  # Đổi từ 3000 sang 3001
JWT_SECRET=local-development-secret-change-in-production
CORS_ORIGIN=http://localhost:5173

# Frontend
VITE_API_URL=http://localhost:3001  # Đổi từ 3000 sang 3001
VITE_KEYCLOAK_URL=http://localhost:8081  # Đổi từ 8080 sang 8081
VITE_KEYCLOAK_REALM=talung
VITE_KEYCLOAK_CLIENT_ID=admin-frontend